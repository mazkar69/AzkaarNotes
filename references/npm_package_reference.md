# 🌟 Useful NPM Packages Reference

Keep this as your go-to cheat sheet while building modern React/Node.js apps.

---

## 📊 Chart Libraries

| Package | Description |
|--------|-------------|
| `react-apexcharts` | Modern charting library for React. Great for dashboards and visualizations. *(Used in Womb)* |
| `highcharts-react-official` | Official Highcharts wrapper for React, supports advanced chart types. *(Used in Gamerlink)* |
| `mermaid` | JavaScript-based diagramming and charting tool. Creates flowcharts, sequence diagrams, Gantt charts, and more using simple markdown syntax. |

---

## 🔁 Development Tools

| Package | Description |
|--------|-------------|
| `react-hot-reload` | Enables instant React component updates in development without page refresh. |
| `cron` | Run scheduled tasks/jobs using CRON expressions in Node.js apps. |
| `clsx` | Utility for conditionally joining classNames. Clean alternative to `classnames`. |
| `tailwind-merge` | Prevents Tailwind CSS class conflicts by intelligently merging them. |

---

## 🧩 UI Components & Styling

| Package | Description |
|--------|-------------|
| `material-tailwind` | Beautiful Material UI components built on top of TailwindCSS. |
| `radix-ui` | Primitives for building accessible and customizable UI components. |
| `lucide-react` | Clean, consistent SVG icon library for React (e.g. `<Mail />`, `<Github />`). |
| `vaul` | Simple drawer component useful for bottom sheets and modals. |
| `cmdk` | Command palette component for keyboard-driven UIs. |
| `react-resizable-panels` | Resizable layout components, great for IDE-style interfaces. |
| `framer-motion` | Production-grade motion library for animations and page transitions. |
| `class-variance-authority` | Create variant-based class APIs for components. |
| `next-themes` | Plug-and-play dark/light mode toggle support for Next.js. |
| `sonner` | Opinionated toast notification library with sleek design. |

---

## 📆 Date & Time Utilities

| Package | Description |
|--------|-------------|
| `react-date-range` | Date range picker component with custom styling support. |
| `react-datepicker` | Lightweight and customizable date picker for React. |
| `react-day-picker` | Powerful date selection library for React, highly customizable. |
| `dayjs` | Lightweight alternative to Moment.js for date manipulation. |
| `moment.js` | Popular library for date/time formatting and parsing. *(Explore/legacy use)* |
| `date-fns` | Modern, modular date utility library for formatting, comparing, etc. |

---

## 🧰 Forms & Validation

| Package | Description |
|--------|-------------|
| `react-hook-form` | Performant form handling in React with minimal re-renders. |
| `zod` | Type-safe schema validation. Pairs perfectly with `react-hook-form`. |
| `express-validator` | Middleware to validate/sanitize request inputs in Express. |
| `input-otp` | Stylish and accessible OTP (One-Time Password) input field for forms. |

---

## 🖼️ Media & Canvas

| Package | Description |
|--------|-------------|
| `yet-another-react-lightbox` | Highly customizable lightbox/gallery component for images. |
| `cloud-convert` | Convert files between formats via API. Useful for file handling and download. |
| `three.js` | 3D rendering library for web — supports 3D scenes, objects, animations. |
| `fabric.js` | Canvas library that simplifies drawing and manipulation (objects, shapes, etc). |
| `react-lineto` | Draws SVG lines between React components or divs. Ideal for flow diagrams. |

---

## 🧲 Drag & Drop

| Package | Description |
|--------|-------------|
| `@dnd-kit/core` | Lightweight and modern drag-and-drop toolkit for React. |

---

## 🔐 Security & Utility

| Package | Description |
|--------|-------------|
| `hashids` | Generate short, unique, non-sequential IDs from numbers (e.g. Mongo IDs). |
| `slugify` | Converts strings to URL-friendly slugs (e.g. `React App` → `react-app`). |

---

## 🎠 Sliders & Carousels

| Package | Description |
|--------|-------------|
| `slick-carousel` | Fully responsive carousel with swipe support and custom controls. |
| `embla-carousel-react` | Modern, lightweight carousel/slider with excellent performance. |

---

